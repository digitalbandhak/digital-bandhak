[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mysqli", "php82Extensions.gd", "php82Extensions.mbstring", "php82Extensions.curl", "php82Extensions.fileinfo", "php82Extensions.zip"]

[phases.build]
cmds = ["mkdir -p uploads/pawns uploads/profiles uploads/chat"]

[start]
cmd = "php -S 0.0.0.0:$PORT router.php"
